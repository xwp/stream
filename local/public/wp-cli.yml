url: stream.wpenv.net
path: wp

# Define the default multisite install config.
core multisite-install:
  title: Stream Development
  admin_user: admin
  admin_password: password
  admin_email: admin@example.com
  skip-config:
  subdomains:
